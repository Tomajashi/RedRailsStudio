import { createReducer, on } from '@ngrx/store';
import { updateresources} from '../action/app.action';
import { Resources } from '../models/resources.model';

export const initialResourcesState: Resources = {

};